// const { ethers } = require("hardhat");
// // const {expect} = require("@nomicfoundation/hardhat-chai-matchers"); 
// const {expect} = require("chai"); 

// beforeEach(async () => {
//     const [owner, otherAddresses] = await ethers.getSigners();

//     const Token = await ethers.deployContract("Market"); // instance contract

//     console.log("token : ", Token)

//     const hardhatToken = await Token.deploy(); // deploy
//     // await hardhatToken.deployed(); 
//     // console.log(hardhatToken);

// }); 

// describe('Test NFT Marketplace ', () => {
//     it("Should deploy the NFT marketplace contract and set the values for the symbol and name", () => {
//         // const name = hardhatToken.name(); 
//         // const symbol = hardhatToken.symbol(); 

//         // expect(name).to.equal("Marketplace"); 
//         // expect(symbol).to.equal("MKP"); 
//     })
// });
